/*
    NULL Pointer :
    --> This pointer do not points to any location 
    --> declaring any pointer without initiallsing points to a random location 
        which can be avoided by using NULL pointer
    --> we can't deference NULL pointer because it don't points to any location 
*/

#include<iostream>
using namespace std;
int main()
{
    int *ptr=NULL;
}