/*
    method -3 :-
    --> two pointer approach:- so we won't store left and right arrays 
    --> this reduces the space complexity from O(n) to O(1)
    --> define L = 0 , r = ht.size() -1  as left and right pointer
    --> define lmax =0, rmax =0 as left and right boundries of the current value
*/